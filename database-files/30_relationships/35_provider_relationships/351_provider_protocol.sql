-- Sample data for provider_protocol_record bridge table
-- Create 125+ rows as specified in requirements

USE meditrack;

-- Insert sample provider-protocol relationships here
-- Example:
-- INSERT INTO provider_protocol_record (provider_id, protocol_id)
-- VALUES (1, 1);
